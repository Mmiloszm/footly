import { SetRowsDirective } from './set-rows.directive';

describe('SetRowsDirective', () => {
  it('should create an instance', () => {
    const directive = new SetRowsDirective();
    expect(directive).toBeTruthy();
  });
});
